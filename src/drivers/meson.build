kernel_sources += files([
  'irq' / 'irq.c',
  'timer' / 'timer.c',
  'uart' / 'uart.c',
])

kernel_includes += [
  'src' / 'include' / 'drivers'/ 'gpio.h',
  'src' / 'include' / 'drivers'/ 'irq.h',
  'src' / 'include' / 'drivers'/ 'timer.h',
  'src' / 'include' / 'drivers'/ 'uart.h',
]
