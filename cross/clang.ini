[binaries]
c       = ['clang', '--target=' + arch]
ld      = 'lld'
ar      = 'llvm-ar'
strip   = 'llvm-strip'
objcopy = 'llvm-objcopy'

[built-in options]
c_args = [] + arch_c_args

[properties]
exec_link_args = [] + arch_exec_link_args
