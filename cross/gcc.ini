[binaries]
c       = arch + '-gcc'
ld      = arch + '-ld'
ar      = arch + '-gcc-ar'
strip   = arch + '-strip'
objcopy = arch + '-objcopy'

[built-in options]
c_args = [ '-fstack-clash-protection' ] + arch_c_args

[properties]
exec_link_args = [ '-nostartfiles' ]
